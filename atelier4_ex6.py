
def sort_list(liste: list) -> list:
    nouvelle_liste = liste[:]
    for i in range(1,len(nouvelle_liste)):
        if nouvelle_liste[i-1] > nouvelle_liste[i]:
            
        else :
            triee = False
            i = len(nouvelle_liste)